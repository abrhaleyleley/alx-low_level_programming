#include "main.h"
#include <string.h>
/**
 *_strlen -> function to get the lenght of a string
 *@s: string to pass to the function
 *Return: lenght of the string
 */
int _strlen(char *s)
{

	return (strlen(s));
}
